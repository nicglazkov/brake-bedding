# Build output
build/
.gradle/
.cxx/
.externalNativeBuild/
captures/
*.apk
*.aab
*.ap_

# Local machine configuration
local.properties
.DS_Store

# Signing material. The keystore itself lives outside the repository; this is a second
# line of defence in case one is ever generated in place.
keystore.properties
*.jks
*.keystore
*.p12

# NOTE: do not add a blanket *.pro rule here. It matches app/proguard-rules.pro, which
# is a source file the release build needs, and its absence only shows up as a broken
# minified build long after the fact.

# IDE
*.iml
.idea/
!.idea/codeStyles/

# Session scratch
.remember/
