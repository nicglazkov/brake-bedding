<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Compose draws each pixel of this app. Because of this, the platform theme has
      only two functions: the window background before the first frame, and the
      change from the splash screen. The transparent system bars operate together
      with enableEdgeToEdge() in MainActivity.
    -->
    <style name="Theme.BrakeBedding" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <style name="Theme.BrakeBedding.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.BrakeBedding</item>
    </style>
</resources>
