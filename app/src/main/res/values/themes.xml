<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Compose draws every pixel of this app, so the platform theme exists only to hold
      the window background used before the first frame and to hand off from the splash
      screen. Transparent system bars pair with enableEdgeToEdge() in MainActivity.
    -->
    <style name="Theme.BrakeBedding" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <style name="Theme.BrakeBedding.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.BrakeBedding</item>
    </style>
</resources>
